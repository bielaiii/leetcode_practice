#include<iostream>
#include<vector>
using namespace std;

string largestMultipleOfThree(vector<int>& digits) {
/*     vector<vector<int>> dp(3, vector<int>(digits.size(), 0));
    sort(digits.begin(), digits.end(), [](char a, char b){
        return a > b;
    });
    if (digits.size() == 1) return digits[0] % 3 == 0 ? to_string(digits[0]) : "";
    string ans = "";
    
    for  */
    return "";
}