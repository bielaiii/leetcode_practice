{
    "version": "2.0.0",
    "tasks": [
        
        {
            "type": "shell",
            "label": "build_cur",
            "command": "g++",
            "args": [
                "-g",
                "-std=c++20",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "problemMatcher": "$gcc"
        },
        
    ]
}