#include"header.h"
#include <cassert>
#include <format>
using namespace std;


int main(void){
     // std::string message = std::format("The answer is {}.", 42);
  //  assert( message == "The answer is 42." );
      cout << "hello, world" <<endl;
    return 0;
}
