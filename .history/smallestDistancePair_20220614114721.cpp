#include<iostream>
#include<vector>
#include<set>
using namespace std;

int smallestDistancePair(vector<int>& nums, int k) {
    int left = 0;
    int right = nums.size() - 1;
    while(left < right){
        int mid = left + (right - left) / 2;
        
    }
}