#include<iostream>
#include<vector>
using namespace std;


bool validPartition(vector<int>& nums){
      vector<int> dp (nums.size() + 1, 0);
      for(int i = 0; i < nums.size(); i++){
            if
      }
}

