#include<iostream>
#include<vector>
using namespace std;

int maximalSquare(vector<vector<char>>& matrix){
    int area = 0;
    vector<vector<int>> land (int(matrix.size()), vector<int>(matrix[0].size()),0);
    for

}
