#include<iostream>
#include<vector>
using namespace std;

int maxProfit_iv(vector<int>& prices){
    int buy1 = prices[0];
    int buy2 = prices[0];
    int profit = 0;
    vector<vector<int>> stock (prices.size(), vector<int>(prices.size(), 0));
    for(int i = 0; i < prices.size(); i++){
        profit = max(profit, )
    }
}