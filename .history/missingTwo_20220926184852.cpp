#include<iostream>
#include<vector>
using namespace std;

vector<int> missingTwo(vector<int>& nums){
      nums.push_back(-1);
      nums.push_back(-1);
      for(int i = 0 ; i < nums.size(); i++){
            if(nums[i] != -1){
                  int temp = nums[i];
            }
      }

}