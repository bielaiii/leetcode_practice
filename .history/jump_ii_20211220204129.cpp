#include<vector>
#include<iostream>
using namespace std;


int jump_ii(vector<int>& nums) {
    int step = 0;
    while(step < nums.size() - 1){
        for(int s = 0; s < nums[step])
    }
    return step;
}