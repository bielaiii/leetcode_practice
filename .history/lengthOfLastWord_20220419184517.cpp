#include<iostream>
using namespace std;

int lengthOfLastWord(string s){
    int right = s.length() - 1;
    int right_idx = s.length() - 1;
    for(; right > 0; right --){
        if(s[right] == ' '){
            
        }
    }
}