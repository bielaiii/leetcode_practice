#include <iostream>
#include<vector>
using namespace std;

int getMaxLen(vector<int>& nums) {
    int p = 0;
    int n = 0;
    int z = 0;
    int len = 0;
    for(int i = 0; i < nums.size(); i++){
        if(nums[i] == 0){
            len = max(len, )
        }
    }
}