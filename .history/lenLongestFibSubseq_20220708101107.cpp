#include<iostream>
#include<vector>
using namespace std;

int lenLongestFibSubseq(vector<int>& arr) {
    vector<int> vec(arr.size(), 0);
    int i = 0, j = 0;
    for(; i < arr.size(); i++){
        if(vec[i] < vec[j] ){
            
        }else{

        }   
    }
}