#include<iostream>
#include<vector>
using namespace std;

vector<string> recur(vector<string>& set, string s, int n){
    if(s[n] > 64 && s[n] < 91 ){
        string temp = s;
        temp[s] = s[n] - 
        set.push_back()
    }
}

vector<string> letterCasePermutation(string s) {

}