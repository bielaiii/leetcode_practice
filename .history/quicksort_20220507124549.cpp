#include <iostream>
#include <vector>
using namespace std;


void quicksort(vector<int>& vec, int low, int high){
    if(low == high) return;
    int i = 0,j = 0;
    for(int i = low; i < high; i++){
        
    }
    
}