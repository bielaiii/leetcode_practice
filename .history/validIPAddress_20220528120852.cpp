#include<iostream>
#include<vector>
using namespace std;


string validIPAddress(string queryIP) {
    if(queryIP.find(",")){
        string deli = ",";
        while
    }else if(queryIP.find(":")){
        string deli = ":";
    }
    return "Neither";
}