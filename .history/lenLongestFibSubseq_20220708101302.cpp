#include<iostream>
#include<vector>
using namespace std;

int lenLongestFibSubseq(vector<int>& arr) {
    vector<int> vec(arr.size(), 0);
    int i = 0, j = 0;
    int first = arr[0];
    int second = arr[1];
    for(i = 2; i < arr.size(); i++){
        
    }
}