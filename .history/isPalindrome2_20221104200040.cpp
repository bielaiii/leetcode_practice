#include<iostream>
#include<vector>
using namespace std;

bool isPalindrome2(ListNode* head) {
      int sz = 0;
      ListNode * temp = head;
      while(temp->next){
            temp = temp->next;
      }
      
}