#include<iostream>
#include<vector>
using namespace std;

double formua(int n, double a ){
    return (n- a*(a-1)/2);
}

int consecutiveNumbersSum(int n) {
    int ret = 0;
    int i = 1;
    int sum = 1;
    while( sum /  ){
        if(n % count == i )ret ++;
        i ++;;
        count ++;
    }
    return ret;
}