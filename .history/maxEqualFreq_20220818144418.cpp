#include<iostream>
#include<vector>
#include<unordered_map>
using namespace std;

int maxEqualFreq(vector<int>& nums) {
    int last = nums.size() - 2;
    unordered_map<int, int> counter , freq;
    for(int i =0 ; i < nums.size(); i++){
        if(counter.count(nums[i])){
            
        }
    }
    return 1;
}