#include<iostream>
#include<vector>
#include<map>
using namespace std;

int largestVariance(string s) {
      int sz = s.length();
      for(char x = 'a'; x < 'z'; x++){
            for(char y = 'a'; y < 'z'; y++){
                  if(x ==y ) continue;
                  
            }
      }
      map<char, int> mp;

}