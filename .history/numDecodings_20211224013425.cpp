#include<iostream>
#include<vector>
using namespace std;

int transfer(string s){
    return s - 64
}


int numDecodings(string s){
    vector<int> ans;
    if(s.length() > 1){
        ans[1] = ans[0] +  
    }
    for(int i = 0; i < s.length(); i++){

    }
}