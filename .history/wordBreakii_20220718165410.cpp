#include<iostream>
#include<vector>
using namespace std;


void dfs_wordBreak(string s, vector<string> & wordDict, int i , int j, vector<bool> & visited ){
      if(j == s.size()) return ;
      for(int temp_i  = i; temp_i < s.size(); temp_i ++){
            ;
      }
      
}     


vector<string> wordBreak(string s, vector<string>& wordDict) {
      vector<string> ans;
      vector<bool> visited(s.size(), 0);
      return ans;
}