#include<iostream>
#include<vector>
using namespace std;

string convert(string s, int numRows) {
      vector<string> > vec(numRows,"");
      vector<int> nums (numRows, 0);
      string str = "";
      for(auto c : s){

      }
      return str;
}