#include<iostream>
#include<map>
using namespace std;

string mostCommonWord(string paragraph, vector<string>& banned) {
      map<string, int> mymap;
      for(string s : banned){
            mymap[s] = -1;
      }
      for(string s : )
}