#include "header.h"

bool checkgo_over(int take){

}

int factorial(int len, int final){
    if(len <= 1) return factorial(len-1, final)
    final *= len;
    len -=1;

}
/*
vector<vector<int> > threeSum(vector<int>& nums){
    vector<int> fail_vec;
    int i=0, j = 0, z= 0;
    if(z != nums.back()){
        
    }

}
*/

void printer(){
    cout <<"hello, world!"<<endl;
}