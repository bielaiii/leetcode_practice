#include <iostream>
#include <vector>
#include <map>
using namespace std;

int longestSubstring(string s, int k) {
      int len = 0;
      for(int i = 0; i < s.length(); i++){
            
      }
      return len;
}