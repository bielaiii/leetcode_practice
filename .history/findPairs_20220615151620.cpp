#include<iostream>
#include<vector>
using namespace std;

void dfs(vector<int> const nums, int k, vector<int> temp, vector<vector<int>> &all, vector<bool>  ){
    if(temp.size() == k){
        all.push_back(temp);
    }
    for(int i = 0; i < nums.size(); i++){
        if()
    }
}

int findPairs(vector<int>& nums, int k) {

}